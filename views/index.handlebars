<nav class="navbar navbar-expand-lg navbar-dark bg-info">
  {{!-- <img src="/docs/4.5/assets/brand/bootstrap-solid.svg" width="30" height="30" alt="wrench"> --}}
  <h3 class="text-white">repairTracker</h3>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown"
    aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNavDropdown">
    <ul class="navbar-nav">
      <li class="nav-item">
        <a class="nav-link" href="#received">Received</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="#inProgress">In Progress</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="#onHold">On Hold</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="#onHold">Complete</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="#new">New Work Order</a>
      </li>
    </ul>
  </div>
</nav>

<div class="container">
  <div class="row">
    <h4 id="received">Received:</h4>
    <table class="table">
      <thead class="thead-light">
        <tr>
          <th scope="col col-lg-2">#</th>
          <th scope="col col-lg-2">First</th>
          <th scope="col col-lg-2">Last</th>
          <th scope="col col-lg-2">Contact</th>
          <th scope="col col-lg-2">Issue</th>
          <th scope="col col-lg-2">Actions</th>
        </tr>
      </thead>
      {{#each burgers}}
      {{#if received}}
      {{> burgers/burger-block received=true}}
      {{/if}}
      {{/each}}
    </table>
  </div>
</div>

<div class="container">
  <div class="row">
    <h4 id="inProgress">In Progress:</h4>
    <table class="table">
      <thead class="thead-light">
        <tr>
          <th scope="col">#</th>
          <th scope="col">First</th>
          <th scope="col">Last</th>
          <th scope="col">Contact</th>
          <th scope="col">Issue</th>
          <th scope="col">Actions</th>
        </tr>
      </thead>
      {{#each burgers}}
      {{#if inProgress}}
      {{> burgers/burger-block inProgress=true}}
      {{/if}}
      {{/each}}
    </table>
  </div>
</div>

<div class="container">
  <div class="row">
    <h4 id="onHold">Waiting on Parts:</h4>
    <table class="table">
      <thead class="thead-light">
        <tr>
          <th scope="col">#</th>
          <th scope="col">First</th>
          <th scope="col">Last</th>
          <th scope="col">Contact</th>
          <th scope="col">Issue</th>
          <th scope="col">Actions</th>
        </tr>
      </thead>
      {{#each burgers}}
      {{#if waiting}}
      {{> burgers/burger-block waiting=true}}
      {{/if}}
      {{/each}}
    </table>
  </div>
</div>

<div class="container">
  <div class="row">
    <h4 id="complete">Complete:</h4>
    <table class="table">
      <thead class="thead-light">
        <tr>
          <th scope="col">#</th>
          <th scope="col">First</th>
          <th scope="col">Last</th>
          <th scope="col">Contact</th>
          <th scope="col">Issue</th>
          <th scope="col">Actions</th>
        </tr>
      </thead>
      {{#each burgers}}
      {{#if devoured}}
      {{> burgers/burger-block devoured=true}}
      {{/if}}
      {{/each}}
    </table>
  </div>
</div>

<div class="d-flex justify-content-center">
  <div class="card mb-3">
    <div class="card-header font-weight-bold h4 d-flex justify-content-center" id="new">Input Order:</div>
    <div class="card-body font-weight-bold">

      <form class="create-form">

        <div class="form-group">
          <label for="firstname">First Name:</label>
          <input type="text" id="firstname" name="name">
          <label for="lastname">Last Name:</label>
          <input type="text" id="lastname" name="name">
          <div class="d-flex justify-content-center">
            <label for="tel">Telephone:</label>
            <input type="text" id="tel" name="name">
          </div>
          <label for="issue" class="d-flex justify-content-center">Issue:</label>
          <textarea class="form-control" id="issue" rows="3"></textarea>
        </div>
        <div class="d-flex justify-content-center">
          <button type="submit" class="btn-lg btn-info">Add Customer</button>
        </div>
      </form>
      </form>
    </div>
  </div>

</div>